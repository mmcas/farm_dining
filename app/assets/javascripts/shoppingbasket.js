element = document.getElementById("editShoppingBasket")
if (element) {
  element.addEventListener("click", function( event ) {
    // display the current click count inside the clicked div
    document.getElementById("lol").style.display = 'block';
  }, false);
}
